// ============================================================
// 画面を実際に描画するテスト
//
// なぜ要るか:
//   tsc は型しか見ない。lint は書き方しか見ない。どちらも
//   「開いたら落ちる」を捕まえられない。実際、プロフィールを
//   開くと必ず落ちるビルド（v1.0.2）を App Store まで上げてしまった。
//   画面を1回マウントするだけで即座に分かる種類の壊れ方だった。
//
//   ここでは本物の React で画面を組み立てて、落ちないことだけを見る。
//   見た目の細かい検証はしない。「開ける」ことの確認に絞る。
// ============================================================

module.exports = {
  preset: 'jest-expo',
  setupFilesAfterEnv: ['<rootDir>/src/__tests__/setup.ts'],
  testMatch: ['<rootDir>/src/__tests__/**/*.test.tsx'],

  // 最初の1件は Babel の変換が入るぶん数秒かかる。既定の5秒だと
  // 実行のたびに落ちたり通ったりする。不安定なテストは、無いより悪い。
  // 「遅いから落ちた」を「壊れたから落ちた」と見分けられなくなる。
  testTimeout: 20000,
}
